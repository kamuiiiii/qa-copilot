# <target-name>

> 这是一个被测系统的元信息文件。`/test` 在执行前会读它，用作上下文。
> 复制这份文件到 `targets/<your-name>.md`，把占位符（`<...>`）替换成实际内容；用不到的小节可以留空，但不要删 section 标题。

## 基本信息

- **URL**: <https://...>
- **环境**: <qa / staging / pre-prod / prod>
